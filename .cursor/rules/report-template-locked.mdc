---
description: Report template is final and locked — no edits unless user explicitly asks
alwaysApply: true
---

# Report Template — LIVE & LOCKED

The personalized creator report used in the live flow.

## Locked file

- `app/gouti/report-preview/page.tsx` — `ReportTemplate`, `ReportData`, and all layout/spacing/styling

## Archive / sandbox

- `app/gouti/report-preview2/page.tsx` — previous template. Safe to experiment here.

## Rules

- **Do not edit** the locked file for refactors, polish, drive-by fixes, or unrelated tasks.
- **Do not suggest** layout, spacing, typography, or copy changes unless the user asks.
- **Only modify** when the user **explicitly** requests a change to the report template.

## Status

Promoted from report-preview2 (September 2026). Lewis Hamilton sample data in preview. `/report/[id]` imports this template.
